import styled from 'styled-components';

const StyledGlassLayer = styled.div`
  background: rgba(167, 236, 239, 0.26);
  box-shadow: 0 4px 30px rgba(0, 0, 0, 0.1);
  backdrop-filter: blur(5.4px);

  height: 85vh;
`;

export default StyledGlassLayer;
